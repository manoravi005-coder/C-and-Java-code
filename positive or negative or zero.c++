#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main() {
    int a;
    cin >> a;
    if(a >=1) cout << "positive";
    else if (a == 0) cout << "zero";
    else cout << "negative";
}
